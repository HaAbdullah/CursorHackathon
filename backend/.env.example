PORT=3001

# OpenRouter API key, used to call vision models for label/ingredient reading
OPENROUTER_KEY=

# Optional: override the vision model used for scans (see openrouter.ai/models)
# OPENROUTER_VISION_MODEL=openai/gpt-4o-mini
